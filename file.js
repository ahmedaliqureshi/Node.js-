// 1st we are going to improt async and fs 
import fs from 'fs/promises'; 
import readline from "readline-sync";
// 2nd we are going to create a function 
async function makeFile(){
 try {
    let fileName = 'file.txt'; 
    let fs.writeFilefileContent = 'names are the names';
await (fileName,fileContent);
await fs.writeFile
    
 } catch (error) {
    cosole.log(error);
 }
}